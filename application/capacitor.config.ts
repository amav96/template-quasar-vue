import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.application',
  appName: 'application',
  webDir: 'src-capacitor/www',
  server: {
    // androidScheme: 'https'
  }
};

export default config;
